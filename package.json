{
  "name": "crossguard",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n frontend,server -c cyan,green \"npm run dev --workspace frontend\" \"npm run dev --workspace server\"",
    "start:server": "npm run start --workspace server",
    "preview:frontend": "npm run preview --workspace frontend -- --host 0.0.0.0",
    "build": "npm run build --workspace frontend && npm run build --workspace server",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
